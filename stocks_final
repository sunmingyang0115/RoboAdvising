def make_stocks_final(portfolio):
    Stocks_Final = portfolio
    Stocks_Final.drop(columns=['Price', 'Currency', 'Value', 'Weight'], inplace=True)
    
    return Stocks_Final

make_stocks_final(Portfolio_Final)
